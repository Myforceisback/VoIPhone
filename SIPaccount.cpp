Привет! Да, конечно, можно установить пакеты на компьютер с Ubuntu без интернета, если рядом есть компьютер с Linux Mint (или любой другой дистрибутив Linux) с доступом в интернет. Вот пошаговая инструкция:

### 1. **Скачивание пакетов на компьютере с интернетом (Linux Mint):**
   - Откройте терминал на компьютере с Linux Mint.
   - Установите утилиту `apt-offline`, если её нет:
     ```bash
     sudo apt-get install apt-offline
     ```
   - Создайте файл с списком необходимых пакетов. Например, если нужно установить пакет `vim`, выполните:
     ```bash
     apt-get download vim
     ```
     Если нужно несколько пакетов, перечислите их через пробел:
     ```bash
     apt-get download vim nano htop
     ```
   - Все скачанные пакеты будут сохранены в текущей директории. Скопируйте их на USB-накопитель или внешний жесткий диск.

### 2. **Перенос пакетов на компьютер без интернета (Ubuntu):**
   - Подключите USB-накопитель или внешний жесткий диск к компьютеру с Ubuntu.
   - Скопируйте пакеты в любую директорию на компьютере с Ubuntu.

### 3. **Установка пакетов на компьютере без интернета (Ubuntu):**
   - Откройте терминал на компьютере с Ubuntu.
   - Перейдите в директорию, куда вы скопировали пакеты:
     ```bash
     cd /путь/к/директории/с/пакетами
     ```
   - Установите пакеты с помощью команды `dpkg`:
     ```bash
     sudo dpkg -i *.deb
     ```
     Если возникнут ошибки из-за зависимостей, выполните:
     ```bash
     sudo apt-get install -f
     ```
     Это установит недостающие зависимости, если они уже есть в системе.

### Альтернативный способ: использование `apt-offline`
Если вы хотите использовать `apt-offline` для более удобного управления пакетами:

1. **На компьютере без интернета (Ubuntu):**
   - Установите `apt-offline`:
     ```bash
     sudo apt-get install apt-offline
     ```
   - Создайте сигнатурный файл:
     ```bash
     sudo apt-offline set apt-offline.sig
     ```
   - Скопируйте этот файл на USB-накопитель.

2. **На компьютере с интернетом (Linux Mint):**
   - Вставьте USB-накопитель и скопируйте сигнатурный файл.
   - Скачайте пакеты, используя сигнатурный файл:
     ```bash
     sudo apt-offline get apt-offline.sig --bundle apt-offline.zip
     ```
   - Скопируйте файл `apt-offline.zip` обратно на USB-накопитель.

3. **На компьютере без интернета (Ubuntu):**
   - Вставьте USB-накопитель и скопируйте файл `apt-offline.zip`.
   - Установите пакеты из архива:
     ```bash
     sudo apt-offline install apt-offline.zip
     ```

Этот способ удобен, если нужно установить много пакетов или если есть сложные зависимости.

Удачи! 😊